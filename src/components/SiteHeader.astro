---
import { Image } from 'astro:assets';
import logo from '../assets/logo_voix_lactee_wbg.png';

const base = import.meta.env.DEV
	? '/'
	: import.meta.env.BASE_URL.endsWith('/')
		? import.meta.env.BASE_URL
		: import.meta.env.BASE_URL + '/';

const toBaseHref = (path = '') => `${base}${path}`.replace(/([^:]\/)\/+/g, '$1');

const links = [
	{ href: '', label: 'Accueil' },
	{ href: 'repertoire', label: 'Répertoire' },
	{ href: 'agenda', label: 'Agenda' },
	{ href: 'contact', label: 'Contact' },
	{ href: 'admin/', label: 'Admin' }
];
---

<header class="border-b border-stone-200 bg-white/90 backdrop-blur">
	<div class="mx-auto flex w-full max-w-5xl items-center justify-between px-4 py-4 sm:px-6">
		<a href={toBaseHref()} class="flex items-center gap-2">
			<Image src={logo} alt="Logo Voix Lactée" class="h-8 w-auto" />
			<span class="text-lg font-semibold tracking-wide text-blue-900">Voix Lactée</span>
		</a>
		<nav aria-label="Navigation principale">
			<ul class="flex items-center gap-1 text-sm font-medium text-stone-600">
				{
					links.map((link) => (
						<li>
							<a class="rounded-md px-3 py-2 transition hover:bg-blue-50 hover:text-blue-900" href={toBaseHref(link.href)}>
								{link.label}
							</a>
						</li>
					))
				}
			</ul>
		</nav>
	</div>
</header>
