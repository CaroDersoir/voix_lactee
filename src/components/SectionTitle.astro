---
interface Props {
	title: string;
	subtitle?: string;
}

const { title, subtitle } = Astro.props;
---

<div class="mb-8">
	<h1 class="text-3xl font-semibold tracking-tight text-blue-950 sm:text-4xl">{title}</h1>
	{subtitle && <p class="subtitle mt-3 max-w-3xl">{subtitle}</p>}
</div>
